<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.co.acctmgmt.mapper.SBGTCDMapper">
	<select id="getSBGTCDData" parameterType="String"
		resultType="SBGTCD">
		SELECT * FROM SBGTCD where GROUP_CD=#{GROUP_CD};
	</select>

	<select id="getDetailInfo" parameterType="String"
		resultType="SBGTCD">
		SELECT
		CTL_FG , BGAJUST_FG , TO_DT , BOTTOM_FG ,BIZ_FG
		FROM sbgtcd
		WHERE BGT_CD=#{bgt_Cd};
	</select>
	<update id="updateDetailInfo" paramterType="SBGTCD" resultType=int>
		UPDATE `SBGTCD`
		SET 
		BGT_NM = #{bgt_Nm},
		CTL_FG = #{ctl_Fg},
		BGAJUST_FG = #{bgajust_Fg},
		TO_DT = '20241231',
		BOTTOM_FG = #{bottom_Fg},
		BIZ_FG = #{biz_Fg},
		GR_FG = #{gr_Fg}
		WHERE BGT_CD = #{bgt_cd};
	</update>
</mapper>