<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.co.acctmgmt.mapper.SBGTCDMapper">
	<select id="getDataGridData" resultType="SBGTCD">
		SELECT * FROM SBGTCD ;
	</select>
	<select id="getSBGTCDData" parameterType="String"
		resultType="SBGTCD">
		SELECT * FROM SBGTCD where GROUPCD=#{GROUPCD};
	</select>

	<select id="getDetailInfo" parameterType="String"
		resultType="SBGTCD">
		SELECT
		CTLFG , BGAJUSTFG , TODT , BOTTOMFG ,BIZFG
		FROM sbgtcd
		WHERE BGTCD=#{bgtCd};
	</select>
	<update id="updateDetailInfo" parameterType="SBGTCD">
		UPDATE sbgtcd
		<set>
			<if test="ctlFg != null">CTLFG=#{ctlFg},</if>
			<if test="bgajustFg != null">BGAJUSTFG=#{bgajustFg},</if>
			<if test="toDt != null">TODT=#{toDt},</if>
			<if test="bottomFg != null">BOTTOMFG=#{bottomFg},</if>
			<if test="bizFg != null">BIZFG=#{bizFg},</if>
		</set>
		WHERE bgtCd =#{bgtCd}
	</update>
	<delete id="deleteRow" parameterType="String">
		DELETE FROM sbgtcd
		WHERE bgtCd = #{bgtCd}
	</delete>
</mapper>